import 'package:flutter/material.dart';

/// Composable context-menu primitives with a pointer-origin clip morph, a
/// gliding active row, checkbox and radio choices, keyboard navigation,
/// typeahead, and long-press support.
///
/// Scaffold — port of the source `beui.dev/r/context-menu` entry;
/// implementation pending. See docs/PORTING_SPEC.md §4.
class BeuiContextMenu extends StatelessWidget {
  const BeuiContextMenu({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('BeuiContextMenu — not yet ported'),
    );
  }
}

/// A single selectable row inside a [BeuiContextMenu].
///
/// Scaffold — port of the source `beui.dev/r/context-menu` `ContextMenuItem`
/// entry; implementation pending. See docs/PORTING_SPEC.md §4.
class BeuiContextMenuItem extends StatelessWidget {
  const BeuiContextMenuItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('BeuiContextMenuItem — not yet ported'),
    );
  }
}
