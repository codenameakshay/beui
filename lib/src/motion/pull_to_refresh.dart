import 'package:flutter/material.dart';

/// Native-feeling pull-to-refresh container with drag resistance, threshold
/// feedback and async refresh handling.
///
/// Scaffold — port of the source `beui.dev/r/pull-to-refresh` entry;
/// implementation pending. See docs/PORTING_SPEC.md §4.
class BeuiPullToRefresh extends StatelessWidget {
  const BeuiPullToRefresh({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('BeuiPullToRefresh — not yet ported'),
    );
  }
}
