import 'package:flutter/material.dart';

/// Inline citation markers paired with a collapsible, progressively rendered reference collection for grounded agent responses.
///
/// Scaffold — port of the source `beui.dev/r/citations` entry; implementation
/// pending. See docs/PORTING_SPEC.md §4.
class BeuiCitations extends StatelessWidget {
  const BeuiCitations({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('BeuiCitations — not yet ported'),
    );
  }
}
