import 'package:flutter/material.dart';

/// A stable generated-image surface that moves from queued work through progressive refinement to a completed result without layout shift.
///
/// Scaffold — port of the source `beui.dev/r/image-generation` entry; implementation
/// pending. See docs/PORTING_SPEC.md §4.
class BeuiImageGeneration extends StatelessWidget {
  const BeuiImageGeneration({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('BeuiImageGeneration — not yet ported'),
    );
  }
}
