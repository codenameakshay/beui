import 'package:flutter/material.dart';

/// A syntax-highlighted file change disclosure with progressive rows, line numbers, live change counts, smooth following, and completion collapse.
///
/// Scaffold — port of the source `beui.dev/r/file-diff` entry; implementation
/// pending. See docs/PORTING_SPEC.md §4.
class BeuiFileDiff extends StatelessWidget {
  const BeuiFileDiff({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('BeuiFileDiff — not yet ported'),
    );
  }
}
