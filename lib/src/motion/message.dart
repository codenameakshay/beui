import 'package:flutter/material.dart';

/// Composable conversation primitives for message rows, grouped bubbles,
/// avatars, metadata, live markers, and a mount-only trailing-edge pop-up for
/// newly sent rows.
///
/// Scaffold — port of the source `beui.dev/r/message` entry;
/// implementation pending. See docs/PORTING_SPEC.md §4.
class BeuiMessage extends StatelessWidget {
  const BeuiMessage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('BeuiMessage — not yet ported'),
    );
  }
}

/// A grouped collection of [BeuiMessage] rows with shared alignment,
/// avatars, and metadata.
///
/// Scaffold — port of the source `beui.dev/r/message` entry;
/// implementation pending. See docs/PORTING_SPEC.md §4.
class BeuiMessageGroup extends StatelessWidget {
  const BeuiMessageGroup({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('BeuiMessageGroup — not yet ported'),
    );
  }
}
