import 'package:flutter/material.dart';

/// An auto-growing agent composer with prompt actions, model selection,
/// keyboard submission, and animated send and stop states.
///
/// Scaffold — port of the source `beui.dev/r/prompt-input` entry;
/// implementation pending. See docs/PORTING_SPEC.md §4.
class BeuiPromptInput extends StatelessWidget {
  const BeuiPromptInput({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('BeuiPromptInput — not yet ported'),
    );
  }
}
