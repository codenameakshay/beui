import 'package:flutter/material.dart';

/// A collapsible agent task plan with morphing status marks, a completion
/// count, compact metadata, and smooth list updates.
///
/// Scaffold — port of the source `beui.dev/r/todo-list` entry;
/// implementation pending. See docs/PORTING_SPEC.md §4.
class BeuiTodoList extends StatelessWidget {
  const BeuiTodoList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('BeuiTodoList — not yet ported'),
    );
  }
}

/// A single task row in a [BeuiTodoList] with a morphing status mark,
/// optional progress, and detail metadata.
///
/// Scaffold — port of the source `beui.dev/r/todo-list` entry;
/// implementation pending. See docs/PORTING_SPEC.md §4.
class BeuiTodoItem extends StatelessWidget {
  const BeuiTodoItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('BeuiTodoItem — not yet ported'),
    );
  }
}
