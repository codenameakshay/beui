import 'package:flutter/material.dart';

/// A human-in-the-loop decision surface for approvals, single or multiple-choice questions, custom responses, and multi-step review flows.
///
/// Scaffold — port of the source `beui.dev/r/approval-card` entry; implementation
/// pending. See docs/PORTING_SPEC.md §4.
class BeuiApprovalCard extends StatelessWidget {
  const BeuiApprovalCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('BeuiApprovalCard — not yet ported'),
    );
  }
}
