import 'package:flutter/material.dart';

/// A complete agent conversation workspace composing navigation, messages, streaming, planning, approvals, tools, code, diffs, generated media, sources, and prompt input.
///
/// Scaffold — port of the source `beui.dev/r/chat-app` entry; implementation
/// pending. See docs/PORTING_SPEC.md §4.
class BeuiChatApp extends StatelessWidget {
  const BeuiChatApp({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('BeuiChatApp — not yet ported'),
    );
  }
}
