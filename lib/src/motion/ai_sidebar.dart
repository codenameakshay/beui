import 'package:flutter/material.dart';

/// A collapsible AI workspace sidebar for folders, projects, files, and bookmarks with keyboard navigation, optimistic moves, inline rename, and overflow-aware labels.
///
/// Scaffold — port of the source `beui.dev/r/ai-sidebar` entry; implementation
/// pending. See docs/PORTING_SPEC.md §4.
class BeuiAiSidebar extends StatelessWidget {
  const BeuiAiSidebar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('BeuiAiSidebar — not yet ported'),
    );
  }
}
