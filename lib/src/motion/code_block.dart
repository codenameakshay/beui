import 'package:flutter/material.dart';

/// A syntax-highlighted code surface with stable streaming updates, line
/// numbers, focused lines, smooth following, and copy feedback.
///
/// Scaffold — port of the source `beui.dev/r/code-block` entry;
/// implementation pending. See docs/PORTING_SPEC.md §4.
class BeuiCodeBlock extends StatelessWidget {
  const BeuiCodeBlock({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('BeuiCodeBlock — not yet ported'),
    );
  }
}
