import 'package:flutter/material.dart';

/// A lightweight execution disclosure for syntax-highlighted terminal output and request responses that collapses into a compact completed state.
///
/// Scaffold — port of the source `beui.dev/r/tool-result` entry; implementation
/// pending. See docs/PORTING_SPEC.md §4.
class BeuiToolResult extends StatelessWidget {
  const BeuiToolResult({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('BeuiToolResult — not yet ported'),
    );
  }
}
