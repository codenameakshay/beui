import 'package:flutter/material.dart';

/// A stable response surface with completion actions, rendered content, and an expandable source summary.
///
/// Scaffold — port of the source `beui.dev/r/streaming-response` entry; implementation
/// pending. See docs/PORTING_SPEC.md §4.
class BeuiStreamingResponse extends StatelessWidget {
  const BeuiStreamingResponse({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('BeuiStreamingResponse — not yet ported'),
    );
  }
}
