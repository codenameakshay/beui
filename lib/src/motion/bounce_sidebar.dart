import 'package:flutter/material.dart';

/// A vertical sidebar whose active dot jumps between destinations on a curved,
/// spring-loaded path.
///
/// Scaffold — port of the source `beui.dev/r/bounce-sidebar` entry;
/// implementation pending. See docs/PORTING_SPEC.md §4.
class BeuiBounceSidebar extends StatelessWidget {
  const BeuiBounceSidebar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('BeuiBounceSidebar — not yet ported'),
    );
  }
}
