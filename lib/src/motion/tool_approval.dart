import 'package:flutter/material.dart';

/// A human-in-the-loop permission card for reviewing tool details, allowing once, remembering access, or denying execution.
///
/// Scaffold — port of the source `beui.dev/r/tool-approval` entry; implementation
/// pending. See docs/PORTING_SPEC.md §4.
class BeuiToolApproval extends StatelessWidget {
  const BeuiToolApproval({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('BeuiToolApproval — not yet ported'),
    );
  }
}
