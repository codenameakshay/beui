import 'package:flutter/material.dart';

/// A focused conversational surface with visual tones, independent alignment,
/// grouped messages, expandable content, and interactive link or button
/// support.
///
/// Scaffold — port of the source `beui.dev/r/message-bubble` entry;
/// implementation pending. See docs/PORTING_SPEC.md §4.
class BeuiMessageBubble extends StatelessWidget {
  const BeuiMessageBubble({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('BeuiMessageBubble — not yet ported'),
    );
  }
}
