import 'package:flutter/material.dart';

/// Expressive call-to-action buttons with expanding, hold, and slide
/// interactions.
///
/// Scaffold — port of the source `beui.dev/r/expanding-arrow-button` entry;
/// implementation pending. See docs/PORTING_SPEC.md §4.
class BeuiExpandingArrowButton extends StatelessWidget {
  const BeuiExpandingArrowButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('BeuiExpandingArrowButton — not yet ported'),
    );
  }
}
