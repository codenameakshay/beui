import 'package:flutter/material.dart';

/// One adaptive activity stream for reasoning, searches, tool calls, structured execution traces, or a chronological mix.
///
/// Scaffold — port of the source `beui.dev/r/agent-activity` entry; implementation
/// pending. See docs/PORTING_SPEC.md §4.
class BeuiAgentActivity extends StatelessWidget {
  const BeuiAgentActivity({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('BeuiAgentActivity — not yet ported'),
    );
  }
}
