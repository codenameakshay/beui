import 'package:flutter/material.dart';

/// Three thoughtful loading states for AI interfaces: shimmering status text, live agent progress, and cycling reasoning phrases.
///
/// Scaffold — port of the source `beui.dev/r/loading-states` entry; implementation
/// pending. See docs/PORTING_SPEC.md §4.
class BeuiLoadingStates extends StatelessWidget {
  const BeuiLoadingStates({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('BeuiLoadingStates — not yet ported'),
    );
  }
}
