import 'package:beui/beui.dart';
import 'package:flutter/material.dart';

/// Gallery route for [BeuiExpandingArrowButton].
Widget expandingArrowButtonDemo(BuildContext context) =>
    const _ExpandingArrowButtonDemo();

class _ExpandingArrowButtonDemo extends StatelessWidget {
  const _ExpandingArrowButtonDemo();

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('ExpandingArrowButton — not yet ported'),
    );
  }
}
