import 'package:beui/beui.dart';
import 'package:flutter/material.dart';

/// Gallery route for [BeuiMessageScroller].
Widget messageScrollerDemo(BuildContext context) => const _MessageScrollerDemo();

class _MessageScrollerDemo extends StatelessWidget {
  const _MessageScrollerDemo();

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('MessageScroller — not yet ported'),
    );
  }
}
