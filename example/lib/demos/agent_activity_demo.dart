import 'package:beui/beui.dart';
import 'package:flutter/material.dart';

/// Gallery route for [BeuiAgentActivity].
Widget agentActivityDemo(BuildContext context) => const _AgentActivityDemo();

class _AgentActivityDemo extends StatelessWidget {
  const _AgentActivityDemo();

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('AgentActivity — not yet ported'),
    );
  }
}
