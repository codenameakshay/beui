import 'package:beui/beui.dart';
import 'package:flutter/material.dart';

/// Gallery route for [BeuiImageGeneration].
Widget imageGenerationDemo(BuildContext context) => const _ImageGenerationDemo();

class _ImageGenerationDemo extends StatelessWidget {
  const _ImageGenerationDemo();

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('ImageGeneration — not yet ported'),
    );
  }
}
