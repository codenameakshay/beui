import 'package:beui/beui.dart';
import 'package:flutter/material.dart';

/// Gallery route for [BeuiAnimatedSidebar].
Widget animatedSidebarDemo(BuildContext context) => const _AnimatedSidebarDemo();

class _AnimatedSidebarDemo extends StatelessWidget {
  const _AnimatedSidebarDemo();

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('AnimatedSidebar — not yet ported'),
    );
  }
}
