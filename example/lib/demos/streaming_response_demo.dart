import 'package:beui/beui.dart';
import 'package:flutter/material.dart';

/// Gallery route for [BeuiStreamingResponse].
Widget streamingResponseDemo(BuildContext context) => const _StreamingResponseDemo();

class _StreamingResponseDemo extends StatelessWidget {
  const _StreamingResponseDemo();

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('StreamingResponse — not yet ported'),
    );
  }
}
