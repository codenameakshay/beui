import 'package:beui/beui.dart';
import 'package:flutter/material.dart';

/// Gallery route for [BeuiPullToRefresh].
Widget pullToRefreshDemo(BuildContext context) => const _PullToRefreshDemo();

class _PullToRefreshDemo extends StatelessWidget {
  const _PullToRefreshDemo();

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('PullToRefresh — not yet ported'),
    );
  }
}
