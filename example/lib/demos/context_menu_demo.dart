import 'package:beui/beui.dart';
import 'package:flutter/material.dart';

/// Gallery route for [BeuiContextMenu].
Widget contextMenuDemo(BuildContext context) => const _ContextMenuDemo();

class _ContextMenuDemo extends StatelessWidget {
  const _ContextMenuDemo();

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('ContextMenu — not yet ported'),
    );
  }
}
