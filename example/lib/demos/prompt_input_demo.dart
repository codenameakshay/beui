import 'package:beui/beui.dart';
import 'package:flutter/material.dart';

/// Gallery route for [BeuiPromptInput].
Widget promptInputDemo(BuildContext context) => const _PromptInputDemo();

class _PromptInputDemo extends StatelessWidget {
  const _PromptInputDemo();

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('PromptInput — not yet ported'),
    );
  }
}
